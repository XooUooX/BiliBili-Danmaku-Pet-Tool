# 安全策略

## 支持范围

当前仅维护最新的 `2.x` 版本。提交安全问题前，请先确认问题仍能在最新代码和最新数据库迁移后复现。

## 报告漏洞

请不要在公开 Issue 中粘贴以下内容：

- B站 Cookie、CSRF Token 或二维码登录信息
- `.env` 文件内容
- 数据库备份
- 支付密钥、OAuth App Key、SMTP 密码、图床 Token
- 可直接利用的完整攻击脚本

建议使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。报告应包含：

1. 受影响版本或提交。
2. 复现条件和最小复现步骤。
3. 影响范围。
4. 可行的修复建议（如有）。
5. 已脱敏的日志或截图。

## 部署安全清单

- 使用随机且足够长的 `SESSION_SECRET`。
- 首次初始化前修改 `ADMIN_PASSWORD`，并在登录后定期更新密码。
- `.env` 权限仅授予服务运行账号。
- 使用 HTTPS，并确保反向代理正确传递 `X-Forwarded-Proto`。
- MySQL 不直接暴露到公网，应用数据库账号采用最小权限。
- 管理后台限制来源 IP 或增加额外身份验证。
- 定期执行 `npm audit --omit=dev` 和 `npm --prefix client audit`。
- 升级前备份数据库，备份文件加密保存。
- 不公开包含用户信息、B站 Cookie 或支付数据的日志。

## 已知部署限制

默认会话存储位于 Node.js 进程内存中，仅适合单实例部署。多实例或高可用部署必须接入共享会话存储。自动调度也应只在一个实例中启用。

本项目依赖第三方平台接口。即使代码没有变化，上游接口、风控策略或签名规则也可能调整。生产使用者应持续监控执行日志，并为资金消耗类任务设置人工确认和合理上限。
